% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pvalues.R
\name{rescale_missing}
\alias{rescale_missing}
\title{Renormalise the eigenvalue spectrum under missing data.}
\usage{
rescale_missing(lambdas_list, fit, df)
}
\description{
For complete data, lavaan's \code{UGamma} spectrum has mean equal to the robust
scaling factor, so the reference law \verb{sum lambda_j chi^2_1} has the right
first moment. Under missing data (FIML) lavaan's \code{UGamma} is mis-scaled (its
mean is off by a constant, ~1.8x in practice), which makes the eigenvalue
p-values badly anti-conservative. The spectrum \emph{shape} is correct, so we
rescale it to mean = \code{chisq.scaling.factor}. This is a no-op for complete
data (where the mean already equals the scaling factor) and requires a robust
test, which FIML fits always have.
}
\keyword{internal}
