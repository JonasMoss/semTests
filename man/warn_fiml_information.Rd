% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{warn_fiml_information}
\alias{warn_fiml_information}
\title{Warn when a FIML fit uses expected information.}
\usage{
warn_fiml_information(fit)
}
\arguments{
\item{fit}{A fitted \code{lavaan} object.}
}
\value{
\code{fit}, invisibly.
}
\description{
Under missing data the expected (Fisher) information is consistent only under
MCAR; under MAR -- the regime FIML is adopted for -- the observed information
is required for valid inference (Kenward & Molenberghs, 1998). lavaan defaults
to observed information for \code{missing = "ml"}, but it silently accepts
\code{information = "expected"}, which would make the eigenvalue p-values rest on
the stronger MCAR assumption. This emits a warning (not an error: expected is
still valid under MCAR).
}
\details{
lavaan stores \code{information} as a length-2 vector, so we test its first entry.
}
\keyword{internal}
