comment: false

coverage:
  status:
    project:
      default:
        # Keep the hardened project baseline steady.
        target: auto
        threshold: 0.1%
    patch:
      default:
        # New source lines should arrive with tests.
        target: 100%
        informational: false
